#include<stdio.h>
int main()
{
    int a,volume;
    printf("Enter the value of side of a cube");
    scanf("%d",&a);
    volume=a*a*a;
    printf("The volume of cube is:%d",volume);
    return 0;
}
